# Copy to `.env` and set your computer's LAN IP (not localhost) so a physical
# phone running Expo Go can reach the Laravel server. Find it with `ipconfig`.
# Restart `npx expo start` after editing.
EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:8000/api
