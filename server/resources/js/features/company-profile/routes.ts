/**
 * Endpoint map for the Company Profile (Company Setup) screen.
 * Mirrors the named routes registered in routes/setup.php.
 */
export const companyProfileRoutes = {
    edit: '/setup/company',
    update: '/setup/company',
} as const;
